export interface Video {
  id: string;
  title: string;
  thumbnail: string;
  publishedAt: string;
  description: string;
  videoUrl: string;
}

export interface NavItem {
  name: string;
  path: string;
}
