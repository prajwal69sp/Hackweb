rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if true; // In a production app, we would restrict this to authenticated admins
    }
  }
}
